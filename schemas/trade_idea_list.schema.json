{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["trade_ideas"],
  "properties": {
    "trade_ideas": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["setup_key", "name", "direction", "conviction", "thesis", "signal_reasons", "entry", "stop", "target", "stretch_target", "time_horizon_days", "invalidation", "confidence_inputs", "historical_context", "backtest_rule", "status"]
      }
    }
  }
}
